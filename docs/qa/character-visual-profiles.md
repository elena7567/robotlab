# Character Visual Profiles

CHARACTER ART / ASSET REVIEWER artifact.

| Profile | Source | Visible Envelope | Antenna Included | Foot Anchor | Decision |
|---|---|---:|---|---:|---|
| helper | `ROBOT_V2_HELPER` alpha bounds in `src/game/assets/characterBounds.ts` | left `-364.892`, top `-1448`, width `729.784`, height `1448` | source helper is complete | `(0, 0)` | Use as `helper`; do not use raw PNG height. |
| assembled | canonical `RobotAssemblyPreview` modular envelope | left `-501.67`, top `-1392`, width `999.7`, height `1382` | YES, includes `robot-v2-antenna` | `(0, 0)` | Use as `assembled` across full-size and assembly-envelope roles. |

Blueprint ghosts, hidden children, debug objects, power glows, and transient effects are excluded from the envelope.
