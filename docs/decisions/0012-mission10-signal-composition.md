# Mission 10 SIGNAL composition rebuild

Date: 2026-09-13
Authority: explicit SIGNAL rebuild request following failed physical Samsung review.

Scope is SIGNAL only. Earlier automated and screenshot PASS claims do not override the user's physical-device failure. Intro, Path, Energy, Launch, Finale and Missions 1–9 retain their behavior and composition.

The primary source of truth is the current written request. Existing approved laboratory, Robot v2 and signal props are exact assets, used directly in a hybrid presentation. Code owns the real beam, rotation feedback, aperture states, input, restored energy feedback and success pulse. The approved reflector texture is cropped into nonoverlapping rotating upper and stationary pedestal sections. No artwork is generated or replaced.

The apparatus and robot form one bounded composition anchored to the central lab platform. SIGNAL uses the existing safe top-center title region with four centered progress dots (three lit). Persistent secondary feedback text is removed. Restored energy remains visible within this composition.

The three authored optical configurations retain two, two and three reflectors and require two, two and three optimal taps. SIGNAL_C places its receiver on the same upper row as the emitter and first mirror (grid y=1 rather than y=0); its actual reflected route and required orientations remain valid. Every tap rotates one mirror immediately; when the beam reaches that mirror its actual traced route changes. An unlit downstream mirror still visibly rotates, while the upstream beam correctly remains unchanged. Incomplete rays retain all reached segments and terminate at the half-cell boundary of the apparatus field. Receiver activation requires an actual traced hit.

Success shows the full route, activated receiver, traveling pulse, subsystem response and robot reaction before the existing Launch stage. Orientation state remains in the existing serializable controller. Responsive metrics are SIGNAL-specific and centralized; other stage layout fields remain unchanged.

Acceptance requires independent mechanics, input/lifecycle, responsive and direct screenshot review, then a production preview for physical Samsung review. Browser emulation cannot grant physical-device PASS.
